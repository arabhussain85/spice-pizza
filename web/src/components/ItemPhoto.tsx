// Menu is text-only (no food photos). Kept as a no-op so existing imports/props
// stay valid while rendering nothing.
export function ItemPhoto(_props: { src?: string | null; alt?: string; className?: string }) {
  return null;
}
